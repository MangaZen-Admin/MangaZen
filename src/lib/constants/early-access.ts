/** Precio por defecto cuando `Chapter.earlyAccessPrice` es null. */
export const DEFAULT_EARLY_ACCESS_PRICE = 50;

export const EARLY_ACCESS_PRICE_MIN = 10;
export const EARLY_ACCESS_PRICE_MAX = 500;

/** Duración Early Access al subir desde Panel Scan (días). */
export const EARLY_ACCESS_DAYS_MIN = 3;
export const EARLY_ACCESS_DAYS_MAX = 30;
